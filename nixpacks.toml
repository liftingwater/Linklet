# Nixpacks configuration for Sevalla deployment
# Specifies the Deno version and startup configuration

[phases.setup]
nixPkgs = ["deno"]

[phases.install]
cmds = ["deno cache src/main.ts"]

[start]
cmd = "deno task start"
